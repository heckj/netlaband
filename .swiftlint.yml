type_name:
  allowed_symbols: "_"
disabled_rules:
  type_body_length
excluded: # paths to ignore during linting. Takes precedence over `included`.
  - netlabandTests
  - netlaband/CircularBuffer.swift
identifier_name:
  min_length: 2
line_length:
  warning: 200
  error: 250
